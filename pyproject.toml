[project]
name = "cal-coder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27",
    "psycopg[binary]>=3.1",
]

[tool.setuptools.packages.find]
include = ["cal_coder*"]
